<dl class="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
  <% if item.data["max_uses"].present? %>
    <div>
      <dt class="text-[var(--text-muted)]">Max Uses</dt>
      <dd class="font-medium text-[var(--text)]"><%= item.data["max_uses"] %></dd>
    </div>
  <% end %>
  <% if item.data["effect"].present? %>
    <div>
      <dt class="text-[var(--text-muted)]">Effect</dt>
      <dd class="font-medium text-[var(--text)]"><%= item.data["effect"] %></dd>
    </div>
  <% end %>
</dl>
