<%# Admin new/edit page header. Locals: title, back_path %>
<div class="mb-4">
  <%= link_to "← Back", back_path, class: "text-[var(--accent)] hover:underline" %>
</div>
<h1 class="text-2xl font-bold text-[var(--text-bright)] mb-6"><%= title %></h1>
