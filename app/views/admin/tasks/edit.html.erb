<div class="mb-4">
  <%= link_to "← Back", [:admin, @resource], class: "text-[var(--accent)] hover:underline" %>
</div>
<h1 class="text-2xl font-bold text-[var(--text-bright)] mb-6">Edit Task</h1>
<%= render "admin/shared/form", model: @resource, fields: [:bsg_id, :full_name, :name, :wiki_link, :given_by, :kappa_required, :lightkeeper_required] %>
