<%= render "admin/shared/form_header", title: "New Previous Task", back_path: [:admin, :previous_tasks] %>
<%= render "admin/shared/form", model: @resource, fields: [
  :requirement_id, :task_id, :task_name
] %>
